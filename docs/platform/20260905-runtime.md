# Runtime maintenance validation — 5 September 2026

The dependency baseline now uses patched js-yaml 4.3.2 for 4.x consumers and
nanoid 3.3.18, with no high/critical advisory allowlist. The paired Antora CLI and
site generator upgrade to 3.2.0 is tested as one candidate. Antora uses its 5.4.x
YAML dependency; Collector 1.0.3 retains the scoped patched 4.x parser.

Local evidence on Node 22.23.2 and CPython 3.12:

- Clean dependency installation and the audit gate passed.
- Normal checks passed: 34 Python tests, 10 AsciiDoc tests, 49 course pages,
  779 mathematical expressions, and generated core notebook execution.
- All 26 full-profile notebooks executed successfully.
- The 102-file student bundle passed manifest and public/private-boundary checks.
- The paired generator built from a linked Git worktree.

This records the maintenance candidate before the new introduction and subsequent
teaching changes. Integrated CI and the final course qualification provide the
updated inventory. Human pilot and annual release decisions remain pending.
