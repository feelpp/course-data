= JAX transformations bridge
:page-course-language: en
:page-course-concepts: DP-JAX-01, DP-JAX-02, DP-JAX-03
:page-course-priority: P2
:page-course-difficulty: D2
:page-course-duration-minutes: 70
:page-course-outcomes: LO8, LO11
:page-course-prerequisites: NumPy arrays, vectorisation, stable loss functions
:page-course-assessed: no

[.course-badge.priority-p2]#P2 Extension# [.course-badge.difficulty-d2]#D2 Independent#

This compact bridge translates familiar NumPy reasoning into JAX transformations. It is optional, CPU-compatible, and excluded from controls and examinations. Removing it leaves the complete 28-hour P0/P1 course unchanged.

== Learning contract

After the lab you should be able to:

* distinguish JAX arrays and pure numerical functions from mutable notebook state;
* own randomness explicitly by creating, splitting, and passing PRNG keys;
* compute a gradient with `grad` or `value_and_grad` and challenge it with finite differences;
* explain the compilation boundary of `jit`, including shape/dtype specialisation and first-call cost;
* replace a Python batch loop with `vmap` and verify result equality.

The lab does not teach neural-network construction, PyTrees, Optax, accelerator setup, or long training loops. Those belong to the xref:extensions/downstream-paths.adoc[Scientific Machine Learning hand-off].

== Notebook

Download xref:attachment$notebooks/extensions/jax-transformations.ipynb[JAX transformations for data scientists]. Install the optional CPU environment first:

[source,console]
----
npm run setup:extensions
----

The course pins JAX `0.10.2`. The official https://docs.jax.dev/en/latest/installation.html[JAX installation guide] documents supported CPU platforms and separate accelerator options; no accelerator is required here.

== Verification discipline

Automatic differentiation differentiates the program that was written. It does not establish that the program represents the intended model. Use a tiny deterministic oracle, compare against central differences, check shapes and dtypes, and then test a transformed batch against a simple scalar version.

Compilation timing is descriptive only. Warm up deliberately, synchronise asynchronous results when benchmarking, and never infer an algorithmic speedup from a single first call.

== Transfer

Implement the stable binary logistic loss from the P1 core with `jax.numpy`. Compute and verify one parameter gradient, vectorise per-example losses and gradients, and record the function's inputs, output shape, dtype, PRNG ownership, and compilation boundary.
